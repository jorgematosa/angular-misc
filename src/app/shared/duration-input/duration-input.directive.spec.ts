import { DurationInputDirective } from './duration-input.directive';

describe('DurationInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DurationInputDirective();
    expect(directive).toBeTruthy();
  });
});
